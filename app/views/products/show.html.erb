<h1><%= @product.name %></h1>
<h3><%= @product.price %></h3>
<h3><%= @product.description %></h3>

<%= link_to "Back to Products", products_path %>
<%= button_to "X", product_path(@product), method: :delete, data: {confirm: "You sure?"} %>

